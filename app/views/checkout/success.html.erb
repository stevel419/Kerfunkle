<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto text-center">
    <div class="bg-white rounded-lg shadow-md p-8">
      <div class="mb-6">
        <svg class="mx-auto h-16 w-16 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
      </div>

      <h1 class="text-3xl font-bold text-gray-900 mb-4">Order Confirmed!</h1>
      <p class="text-gray-600 mb-8">
        Thank you for your order! Your payment has been processed successfully.
      </p>

      <% if logged_in? %>
        <div class="bg-blue-50 border border-blue-200 rounded-md p-4 mb-6">
          <p class="text-blue-800">
            Your order has been saved to your account. You can view your order history at any time.
          </p>
        </div>
      <% else %>
        <div class="bg-yellow-50 border border-yellow-200 rounded-md p-4 mb-6">
          <p class="text-yellow-800">
            Your order was placed as a guest. Consider creating an account to track your orders.
          </p>
        </div>
      <% end %>

      <div class="space-y-4">
        <%= link_to "Continue Shopping", products_path,
            class: "w-full bg-purple-600 text-white py-3 px-6 rounded-md hover:bg-purple-700" %>

        <% if logged_in? %>
          <%= link_to "View Order History", orders_path,
              class: "w-full bg-gray-200 text-gray-800 py-3 px-6 rounded-md hover:bg-gray-300" %>
        <% else %>
          <%= link_to "Create Account", "#",  # Will be implemented in Phase 3
              class: "w-full bg-gray-200 text-gray-800 py-3 px-6 rounded-md hover:bg-gray-300" %>
        <% end %>
      </div>

      <div class="mt-8 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-500">
          Order confirmation has been sent to your email.
        </p>
      </div>
    </div>
  </div>
</div>